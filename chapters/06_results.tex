\chapter{Results and Evaluation}\label{chap:results-evaluation}

This chapter answers the research questions of \cref{sec:gqm-decomposition} from the measurements produced by the campaign of \cref{chap:implementation}. Its structure follows the \gls{gqm} decomposition directly: \crefrange{sec:rq1}{sec:rq5} take the five recurring questions in the order of \cref{tab:research-questions}, and each answers its question for both goals at once. Because the same question is asked under each goal and differs only in the identity configuration, the identity-enabled ($G_1$) and identity-disabled ($G_2$) values of every metric appear in the same table, with their difference reported beside them. \Cref{sec:x1-overhead} then draws the cross-goal result X1 together across all five questions and interprets it, together with the connector-to-connector differences, against the documented design of each implementation; \cref{sec:answers-rq} consolidates that interpretation into the answer to \textbf{TRQ2}. \Cref{sec:results-validity} states the threats to the validity of both, and \cref{sec:conclusion-limitations} states how far the results carry.

Dashboard panels rendered from the live monitoring stack during these runs are collected in \cref{app:panels}; they illustrate the instrument and the shapes over time that a summary statistic discards, and are not the source of any figure reported here.

The statistics used throughout, the meaning of $\Delta$, and the rules by which runs are admitted or excluded are defined in \cref{subsec:analysis-statistics}; they are applied uniformly to every connector and both arms, with no per-connector special-casing.

\section{RQ1 --- Latency and Throughput at the Operating Point}\label{sec:rq1}

% G_k.RQ1: per-phase and end-to-end latency, and sustained throughput, at a realistic
% operating point. Sources: rq1-steady.csv, rq1-payload.csv.
% Phase decomposition is RQ4 (sec:rq4); this section reports the end-to-end figure.
\Cref{tab:rq1-steady} reports end-to-end transaction latency and sustained throughput at the fixed operating point. This is the only load offered identically to every connector, and therefore the only basis on which a head-to-head comparison is legitimate; the saturation ladders of \cref{sec:rq2} are per-connector by construction and comparing across them would compare different offered loads.

\begin{table}[H]
    \centering
    \begin{threeparttable}
    \small
    \setlength{\tabcolsep}{4pt}
    \input{tables/rq1-steady}
    \begin{tablenotes}\footnotesize
        \item Latency columns in ms. Throughput is completed transactions per second over the measured window, not the rate the load generator was configured to offer.
        \item $\Delta = \tilde{x}_{G_1} - \tilde{x}_{G_2}$ of the median, reported on the $G_1$ row (\cref{subsec:analysis-statistics}).
    \end{tablenotes}
    \end{threeparttable}
    \caption[Operating-point latency and throughput]{End-to-end latency and sustained throughput at the operating point, by connector and identity goal.}
    \label{tab:rq1-steady}
\end{table}

\textbf{Throughput does not distinguish the connectors at the operating point.} Every arm delivered the offered load: completed transactions range from 0.915 to 0.950\,tx/s against an offered 1\,tx/s, a spread of under four per cent across all six arms. It is reported for completeness rather than as a result; capacity is the subject of \cref{sec:rq2}.

\textbf{Latency does distinguish them.} Under $G_1$ the two \gls{edc} connectors are indistinguishable from one another -- 4672.5\,ms for the Fraunhofer ISST \gls{edc} against 4649.0\,ms for Factory-X, a difference of 23.5\,ms, or 0.5\,\% -- while DSP-Native BaSyx completes the same transaction in 2884.5\,ms, some 38\,\% faster. Under $G_2$ the ordering is preserved but the separation widens: 4198.0\,ms and 4416.0\,ms for the \gls{edc} pair against 814.0\,ms for BaSyx, which is then roughly one fifth of either.

\textbf{Five repetitions bound the resolution of every comparison.} Run-to-run variation of the median, expressed as the relative range, is between 0.6\,\% and 9.2\,\%, and it is consistently smaller in the identity-disabled arms (0.6--2.6\,\%) than in the identity-enabled ones (4.8--9.2\,\%). Every comparison drawn from this table is bounded by those figures: the 23.5\,ms separating the two \gls{edc} connectors under $G_1$ is far below the 4.8--5.4\,\% variation of either, and is not interpreted as a difference. The ratio of mean to median lies between 0.97 and 1.01 in all six arms, so the distributions are close to symmetric at this load and the choice of median over mean changes nothing here; that choice earns its place under load rather than at the operating point (\cref{subsec:analysis-statistics}).

\Cref{fig:rq1-payload} reports data-plane capacity against payload size. This scenario negotiates once and then repeats only the transfer, so its unit of observation is a pull rather than a transaction. DSP-Native BaSyx collapses control and data plane into one process and has no separate data plane to exercise, so it is absent from the figure by construction (\cref{sec:sut}).

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
    \begin{axis}[thesisplot, height=5.6cm, xlabel={Payload size (bytes)},
                 ylabel={Achieved rate (MB/s)}, xmode=log, log basis x=10,
                 ymode=log, log basis y=10, legend pos=north west]
      \addplot[isst,g1] table [x=bytes_per_pull,y=throughput_MB_s,col sep=comma] {tables/fig-pay-dst-on.csv};
      \addplot[isst,g2] table [x=bytes_per_pull,y=throughput_MB_s,col sep=comma] {tables/fig-pay-dst-off.csv};
      \addplot[fx,g1]   table [x=bytes_per_pull,y=throughput_MB_s,col sep=comma] {tables/fig-pay-factoryx-on.csv};
      \addplot[fx,g2]   table [x=bytes_per_pull,y=throughput_MB_s,col sep=comma] {tables/fig-pay-factoryx-off.csv};
      \legend{ISST $G_1$, ISST $G_2$, Factory-X $G_1$, Factory-X $G_2$}
    \end{axis}
    \end{tikzpicture}
    \caption[Data-plane rate against payload size]{Achieved data-plane transfer rate
    against payload size, by connector and identity goal. Both axes are logarithmic. The
    four series are close to coincident, which is the data-plane form of the identity
    result reported in \cref{sec:x1-overhead}.}
    \label{fig:rq1-payload}
\end{figure}

\textbf{Data-plane throughput rises monotonically with payload size and saturates at the largest transfers.} For the Fraunhofer ISST \gls{edc} it climbs from 0.27\,MB/s at 1\,KB to 515.1\,MB/s at 100\,MB, and for Factory-X from 0.26\,MB/s to 458.2\,MB/s. Small payloads are dominated by per-request overhead rather than by transfer -- a 1\,KB pull takes 3.7--3.9\,ms and a 100\,KB pull 4.0--4.3\,ms, so a hundredfold increase in payload costs under ten per cent in time -- while beyond 1\,MB the pull duration grows in proportion to size.

\textbf{The identity arms are indistinguishable throughout.} Across all five sizes and both connectors the largest difference in pull latency between $G_1$ and $G_2$ is 7.5\,ms, on the 100\,MB transfer, and the achieved rates differ by at most 3.7\,\%, which is within the run-to-run variation reported in \cref{tab:rq1-steady}. The verified byte counts confirm that each run transferred the asset its label names, ruling out a substituted asset as an explanation for the agreement.

\section{RQ2 --- Behaviour under Rising Load and Concurrency}\label{sec:rq2}

% G_k.RQ2: latency and success rate as offered load and concurrency rise -- the
% saturation knee and the onset of failure. Sources: rq2-saturation.csv,
% rq2-concurrency.csv.

\Cref{fig:rq2-saturation} reports the open-model saturation ladder for both goals. Offered load rather than client population is the controlled variable, which avoids the coordinated-omission bias to which a closed model is subject when the server slows~\citep{schroeder2006open}. The offered rate of each rung is the mean arrival rate over that stage rather than its nominal target, because the load generator interpolates linearly towards the target across the stage; comparing delivery against the nominal value understates it on every ramped rung~\citep{k6docs}.

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
    \begin{axis}[thesisplot, xlabel={Offered rate (tx/s)}, ylabel={Delivered rate (tx/s)},
                 xmin=0.5, xmax=6, ymin=0, legend pos=north west, legend columns=2]
      \addplot[isst,g1]  table [x=offered_tx_s,y=delivered_tx_s,col sep=comma] {tables/fig-sat-dst-on.csv};
      \addplot[isst,g2]  table [x=offered_tx_s,y=delivered_tx_s,col sep=comma] {tables/fig-sat-dst-off.csv};
      \addplot[fx,g1]    table [x=offered_tx_s,y=delivered_tx_s,col sep=comma] {tables/fig-sat-factoryx-on.csv};
      \addplot[fx,g2]    table [x=offered_tx_s,y=delivered_tx_s,col sep=comma] {tables/fig-sat-factoryx-off.csv};
      \addplot[basyx,g1] table [x=offered_tx_s,y=delivered_tx_s,col sep=comma] {tables/fig-sat-basyx-on.csv};
      \addplot[basyx,g2] table [x=offered_tx_s,y=delivered_tx_s,col sep=comma] {tables/fig-sat-basyx-off.csv};
      \addplot[gray,dotted,no marks,forget plot] coordinates {(0.5,0.5) (6,6)};
      \legend{ISST $G_1$, ISST $G_2$, Factory-X $G_1$, Factory-X $G_2$, BaSyx $G_1$, BaSyx $G_2$}
    \end{axis}
    \end{tikzpicture}
    \caption[Delivered against offered transaction rate]{Delivered against offered
    transaction rate per ladder rung, by connector and identity goal. Each point is the
    median across three repetitions. The dotted diagonal is perfect delivery; departure
    from it locates the saturation point. Delivered counts completions rather than
    successes, so transactions abandoned at the poll deadline are included.}
    \label{fig:rq2-saturation}
\end{figure}

\textbf{Delivery tracks the offered rate to 2.5\,tx/s.} All six arms deliver between 89\,\% and 99\,\% of a mean offered rate through the first three rungs. The ladder separates them at the fourth rung, an offered mean of 4\,tx/s. There the two BaSyx arms and both Fraunhofer ISST arms still deliver 83--87\,\%, whereas Factory-X falls to 50--54\,\% in both arms -- it delivers 2.0--2.2\,tx/s while 4\,tx/s is offered, and does not exceed that figure at any higher rung. Factory-X identity-enabled is the only arm to breach the 90\,\% criterion earlier, at the third rung, with 89\,\%.

\textbf{Beyond the fourth rung the arms diverge in shape as well as level.} The Fraunhofer ISST \gls{edc} plateaus: delivered throughput settles at 3.9--4.1\,tx/s in both arms and does not rise further as the offered rate is increased to 12\,tx/s. DSP-Native BaSyx continues to climb, reaching 6.0\,tx/s identity-disabled and 5.9\,tx/s identity-enabled at the top rung, with the delivery ratio falling smoothly rather than abruptly. Factory-X shows neither pattern cleanly; its delivered rate at the highest rungs is non-monotonic, which \cref{sec:results-validity} addresses.

\textbf{Delivery counts completions, not successes.} These figures therefore include transactions abandoned at the poll deadline. The proportion so abandoned over the whole ladder is 22--34\,\% for the Fraunhofer ISST and BaSyx arms and 50\,\% for both Factory-X arms.

\Cref{fig:rq2-concurrency} reports the closed-model ladder as an independent check on the same ceiling. Under a closed model a saturated server shows throughput flattening while residence time grows in proportion to the client population, which is the form Little's law takes at saturation~\citep{little1961proof}; agreement between the two workload models is what makes an observed ceiling a property of the connector rather than of one model.

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
    \begin{axis}[thesisplot, xlabel={Virtual users}, ylabel={Delivered rate (tx/s)},
                 xmode=log, log basis x=2, ymin=0, legend pos=north west, legend columns=2]
      \addplot[isst,g1]  table [x=virtual_users,y=delivered_tx_s,col sep=comma] {tables/fig-conc-dst-on.csv};
      \addplot[isst,g2]  table [x=virtual_users,y=delivered_tx_s,col sep=comma] {tables/fig-conc-dst-off.csv};
      \addplot[fx,g1]    table [x=virtual_users,y=delivered_tx_s,col sep=comma] {tables/fig-conc-factoryx-on.csv};
      \addplot[fx,g2]    table [x=virtual_users,y=delivered_tx_s,col sep=comma] {tables/fig-conc-factoryx-off.csv};
      \addplot[basyx,g1] table [x=virtual_users,y=delivered_tx_s,col sep=comma] {tables/fig-conc-basyx-on.csv};
      \addplot[basyx,g2] table [x=virtual_users,y=delivered_tx_s,col sep=comma] {tables/fig-conc-basyx-off.csv};
      \legend{ISST $G_1$, ISST $G_2$, Factory-X $G_1$, Factory-X $G_2$, BaSyx $G_1$, BaSyx $G_2$}
    \end{axis}
    \end{tikzpicture}
    \caption[Throughput against client concurrency]{Delivered transaction rate against
    client concurrency (closed model), by connector and identity goal. A series that
    continues to rise is not yet the constraint; one that flattens while the client
    population grows is saturated, with the difference absorbed by residence time.}
    \label{fig:rq2-concurrency}
\end{figure}

\textbf{The closed model agrees with the open one on which arms are saturated.} DSP-Native BaSyx identity-disabled scales close to linearly -- 3.05, 8.51, 17.49 and 39.49\,tx/s at 5, 10, 20 and 50 virtual users -- while its implied residence time stays flat between 0.82 and 0.90\,s. A client population that grows tenfold without lengthening the transaction is the signature of a server that is not the constraint.

\textbf{Every other arm shows the opposite.} DSP-Native BaSyx identity-enabled delivers 0.89, 1.31, 2.39 and 3.83\,tx/s over the same ladder while implied residence time rises from 2.82 to 9.13\,s; the Fraunhofer ISST identity-disabled arm delivers 0.51, 1.24 and 2.28\,tx/s with residence time rising from 4.89 to 6.58\,s. Throughput growing more slowly than the client population, with residence time absorbing the difference, is the form Little's law takes at saturation~\citep{little1961proof}.

\section{RQ3 --- Resource Consumption}\label{sec:rq3}

% G_k.RQ3: CPU, JVM heap, GC, threads under identical workloads, plus the host
% ceiling. Sources: rq3-resources.csv, rq3-drift.csv.

\Cref{tab:rq3-resources} reports host and \gls{jvm} resource consumption under the same workloads. Peaks are reported alongside medians because the question this data must answer is whether an observed throughput ceiling was imposed by the host or by the connector: a connector that saturates while the host retains headroom is limited by its own structure, which is an architectural finding rather than a hardware one~\citep{gregg2012thinking}.

\begin{table}[H]
    \centering
    \begin{threeparttable}
    \small
    \setlength{\tabcolsep}{5pt}
    \input{tables/rq3-resources}
    \begin{tablenotes}\footnotesize
        \item \Gls{cpu} figures cover the whole host; only one connector was deployed at a time (\cref{subsec:hardware}).
        \item Heap peak is the maximum across the connector's instrumented services.
    \end{tablenotes}
    \end{threeparttable}
    \caption[Host and JVM resource consumption]{Host and \gls{jvm} resource consumption under identical workloads, by connector and identity goal.}
    \label{tab:rq3-resources}
\end{table}

\textbf{Host \gls{cpu} never approached exhaustion.} Peak busy time across the measured windows is 48.1\,\% for the Fraunhofer ISST identity-enabled arm, 42.7\,\% for Factory-X and 31.6\,\% for BaSyx, with medians between 2.3\,\% and 16.9\,\%. Since every arm reached its delivered-throughput ceiling within these runs (\cref{sec:rq2}), the ceiling is not the host: at the point where each connector stopped converting offered load into completed transactions, at least half of the machine remained idle.

\textbf{The connectors differ in memory and collector behaviour rather than in processor demand.} Peak heap is 725.2\,MB for Factory-X, 665.9\,MB for BaSyx identity-enabled and 508.3\,MB for the Fraunhofer ISST \gls{edc}. Garbage-collection activity separates them more sharply: aggregated across instrumented runtimes it peaks at 0.605\,s/s for Factory-X and 0.320\,s/s for the Fraunhofer ISST \gls{edc}, against 0.011\,s/s for BaSyx -- a difference of nearly two orders of magnitude.

\textbf{The identity layer's effect on memory is confined to one connector.} Peak heap differs between arms by 1.8\,\% for the Fraunhofer ISST \gls{edc} and 0.9\,\% for Factory-X, both within run-to-run variation, whereas BaSyx holds 665.9\,MB identity-enabled against 323.8\,MB identity-disabled, a factor of 2.06.

\textbf{No service grows fast enough to threaten its container limit.} Long-run stability was measured over a thirty-minute endurance run per arm, with heap
occupancy sampled every five seconds for each instrumented runtime and a least-squares
trend fitted to each series. No service in any arm grows fast enough to threaten its
container limit: the fastest-growing series is the Fraunhofer ISST \gls{edc} consumer
control plane under $G_1$ at 19.2\,MB/h, followed by the DSP-Native BaSyx provider at
10.3\,MB/h, while the two Factory-X arms reach only 7.0 and 1.8\,MB/h and every series
in the BaSyx identity-disabled arm is flat or falling. Against heaps bounded at 768\,MB
and observed occupancies between 40 and 190\,MB, none of these rates would exhaust the
available memory during the observation window or soon after it.

\textbf{The window bounds the claim rather than the measurement.} Thirty minutes is sufficient to
show that no component grows catastrophically, and insufficient to exclude a slow leak;
\cref{sec:conclusion-limitations} records that limit.

\section{RQ4 --- The Asynchronous Cycle, Decomposed by Phase}\label{sec:rq4}

% G_k.RQ4: the asynchronous negotiation and transfer cycle, decomposed by phase.
% Source: rq4-phases.csv. This is where X1 is localised to a phase, which is the
% input the architectural interpretation needs.

\Cref{tab:rq4-phases} decomposes the transaction into the phases of the \gls{dsp} cycle. Reporting both goals per phase localises the identity cost to a specific point in the protocol, which is the evidence the architectural interpretation of \cref{sec:x1-overhead} rests on.

\begin{table}[H]
    \centering
    \begin{threeparttable}
    \small
    \setlength{\tabcolsep}{5pt}
    \small
    \setlength{\tabcolsep}{4pt}
    \input{tables/rq4-phases}
    \begin{tablenotes}\footnotesize
        \item Phases are listed in protocol order rather than by magnitude. \emph{Share} is the phase median as a percentage of the end-to-end median under $G_1$.
        \item Medians are not additive: the median of a sum is not the sum of the medians, so the shares are an approximate decomposition rather than an exact time budget. The residual against the end-to-end median of \cref{tab:rq1-steady} is stated in the text.
        \item Phase boundaries are observed by polling at a fixed interval, so each duration carries an upper-bound quantisation error of one poll interval (\cref{subsec:custom-metrics}).
    \end{tablenotes}
    \end{threeparttable}
    \caption[Per-phase latency of the DSP transaction cycle]{Per-phase latency of the \gls{dsp} transaction cycle, by connector and identity goal.}
    \label{tab:rq4-phases}
\end{table}

\textbf{The two asynchronous waits dominate every arm of every connector.} Time-to-agreed accounts for 54.8--61.7\,\% of the end-to-end median and time-to-\gls{edr} for a further 35.3--38.3\,\%; together they are between 93 and 97\,\% of the transaction. The synchronous steps are negligible by comparison: negotiation initiation, transfer initiation and the data pull together account for 1.0--1.7\,\%, and the catalog request for 1.9--2.3\,\%. This holds identically for the two \gls{edc} connectors and for the architecturally different BaSyx provider.

\textbf{Where the identity layer's cost falls differs sharply between connectors.} On the two \gls{edc} connectors it concentrates in the catalog request, which rises from 8.3 to 101.9\,ms on the Fraunhofer ISST \gls{edc} (a factor of 12.2) and from 9.6 to 86.8\,ms on Factory-X (a factor of 9.1), while the two asynchronous waits change by 0.2--10\,\%. On BaSyx the pattern is inverted: the catalog request rises by 47.9\,ms, but time-to-agreed rises from 254 to 1781\,ms and time-to-\gls{edr} from 253 to 1017\,ms, so the cost falls almost entirely inside the asynchronous waits.

\textbf{One phase is consistently faster with identity enabled than without it.} Negotiation initiation takes 6.2\,ms against 50.1\,ms on the Fraunhofer ISST \gls{edc} and 7.0\,ms against 45.8\,ms on Factory-X -- ratios of 0.12 and 0.15 -- and 6.8\,ms against 10.4\,ms on BaSyx. The measurement is reported as observed; \cref{sec:x1-overhead} considers what would account for it.

\textbf{The phase budget reconciles with the end-to-end median.} The residuals are $-1$\,ms for BaSyx, $-77$\,ms for the Fraunhofer ISST \gls{edc} and $+41$\,ms for Factory-X, in every case under two per cent of the end-to-end figure, so the shares above may be read as a time budget rather than only as an ordering. The polling that produced these boundaries cost 10--13 negotiation polls and 6--10 \gls{edr} polls per transaction, which is the observer effect the figures must be read net of; \cref{sec:results-validity} quantifies it directly.

\section{RQ5 --- Catalog-Size Scaling}\label{sec:rq5}

% G_k.RQ5: catalog-request latency against the number of offered assets.
% Source: rq5-catalog.csv. This is the one question where X1 can be shown to be a
% fixed offset rather than a proportional cost.

\Cref{fig:rq5-catalog} reports catalog-request latency against the number of advertised assets. The sweep spans three orders of magnitude, and growth across it is reported as two factors -- the factor by which the catalog grows against the factor by which latency grows -- rather than as a fitted exponent (\cref{subsec:analysis-statistics}). DSP-Native BaSyx exposes no provider management \gls{api} through which a catalog of controlled size can be seeded and is absent from the figure by construction (\cref{sec:sut}).

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
    \begin{axis}[thesisplot, height=5.6cm, xlabel={Catalog size (assets)},
                 ylabel={Catalog request latency (ms)}, xmode=log, log basis x=10,
                 ymin=0, legend pos=north west]
      \addplot[isst,g1] table [x=assets,y=G1_ms,col sep=comma] {tables/fig-cat-dst.csv};
      \addplot[isst,g2] table [x=assets,y=G2_ms,col sep=comma] {tables/fig-cat-dst.csv};
      \addplot[fx,g1]   table [x=assets,y=G1_ms,col sep=comma] {tables/fig-cat-factoryx.csv};
      \addplot[fx,g2]   table [x=assets,y=G2_ms,col sep=comma] {tables/fig-cat-factoryx.csv};
      \legend{ISST $G_1$, ISST $G_2$, Factory-X $G_1$, Factory-X $G_2$}
    \end{axis}
    \end{tikzpicture}
    \caption[Catalog latency against catalog size]{Catalog-request latency against the
    number of advertised assets, by connector and identity goal. The horizontal axis is
    logarithmic. DSP-Native BaSyx exposes no provider management \gls{api} through which
    a catalog of controlled size can be seeded and is absent.}
    \label{fig:rq5-catalog}
\end{figure}

\textbf{Catalog latency is insensitive to catalog size until the largest level.} On the Fraunhofer ISST \gls{edc} the identity-enabled median moves from 426.2\,ms at one asset to 415.0 and 418.3\,ms at ten and one hundred, then to 697.0\,ms at one thousand; Factory-X follows the same shape, from 654.5 through 655.1 and 679.6 to 1153.6\,ms. Over a thousandfold increase in catalog size, request latency grows by a factor of 1.64 and 1.76 respectively.

\textbf{The identity cost across that sweep is close to constant in absolute terms.} For the Fraunhofer ISST \gls{edc} it is 135.8, 124.0, 105.8 and 131.4\,ms at the four sizes, and for Factory-X 92.0, 108.8, 72.3 and 36.1\,ms. Because the underlying latency grows while the difference does not, the same cost expressed as a ratio falls with catalog size: from 1.47 to 1.23 for the Fraunhofer ISST \gls{edc} and from 1.16 to 1.03 for Factory-X.

\section{X1 --- The Measured Cost of Identity}\label{sec:x1-overhead}

% The headline cross-goal result (ch. 4: "the directly measured cost of running real
% identity"). Every Delta above is collected here; nothing new is measured.
% Source: x1-overhead.csv.

The preceding sections report the difference between the two goals question by question. \Cref{tab:x1-overhead} collects those differences into one view, which is what permits the three claims this section has to settle: whether the identity layer charges a fixed cost per transaction or one proportional to the work, at which phase of the protocol that cost is incurred, and whether it extends to the data plane or is confined to the control plane.

\begin{table}[H]
    \centering
    \begin{threeparttable}
    \small
    \setlength{\tabcolsep}{5pt}
    \small
    \setlength{\tabcolsep}{4pt}
    \input{tables/x1-overhead}
    \begin{tablenotes}\footnotesize
        \item Both the difference and the ratio are reported: a fixed offset is a large ratio on a cheap operation and a negligible one on an expensive one, so either number alone conceals whether the cost is constant or proportional.
        \item \emph{Separated} records whether the two goals' observed ranges overlap. It is a weak criterion at this repetition count and is not a significance test (\cref{subsec:analysis-statistics}).
        \item The DSP-Native BaSyx identity-disabled arm uses a different load driver from its identity-enabled arm (\cref{subsec:mock-arm}), so its $\Delta$ confounds the identity layer with a harness difference and is not comparable with the other two connectors' (\cref{sec:results-validity}).
    \end{tablenotes}
    \end{threeparttable}
    \caption[Identity overhead per connector and metric]{Identity overhead X1 per connector and metric, as the difference between the identity-enabled and identity-disabled goals.}
    \label{tab:x1-overhead}
\end{table}

The figures above are measured end to end, so they include whatever the consumer
contributes. For the two \gls{edc} connectors both arms share the same consumer and that
is harmless, but the DSP-Native BaSyx identity-disabled arm replaces the \gls{edc}
consumer with a callback sink (\cref{subsec:mock-arm}), so its end-to-end difference
mixes the identity layer with a different consumer stack. \Cref{tab:x1-provider}
therefore repeats the comparison on the provider's own server-side latency, measured by
the same agent on the same component in both arms, which is the only form of the
measurement that is comparable across all three connectors. The provider's request rate
is reported beside it as a control: equal load with higher latency isolates the
difference to the work done per request.

\begin{table}[H]
    \centering
    \begin{threeparttable}
    \small
    \setlength{\tabcolsep}{4pt}
    \input{tables/x1-provider}
    \begin{tablenotes}\footnotesize
        \item Latency is the server-observed 95th percentile, summarised over the
        measured window and then across repetitions. Request rate is in requests per
        second.
    \end{tablenotes}
    \end{threeparttable}
    \caption[Identity overhead measured on the provider]{Identity overhead measured on
    the provider itself, with the provider's request rate as a load control.}
    \label{tab:x1-provider}
\end{table}

Measured end to end, the identity layer costs the Fraunhofer ISST \gls{edc} 474.5\,ms per
transaction (a factor of 1.11) and Factory-X 233.0\,ms (1.05). Sustained throughput is
unaffected in both: the arms deliver the same rate to within one per cent, because at an
operating point below saturation the offered rate rather than the connector determines
delivery. The tail moves with the median rather than faster than it -- p95 rises by
388.8\,ms and 291.0\,ms respectively -- so identity shifts the distribution rather than
lengthening its tail disproportionately.

Measured on the provider alone the same cost is 30.0\,ms (1.15) and 41.3\,ms (1.22), and
for DSP-Native BaSyx 44.7\,ms (2.90). The load control holds in all three cases: the
provider served the same request rate in both arms to within one per cent, so a higher
latency at equal load isolates the difference to the work performed per request, which is
the credential verification itself.

\textbf{The cost is fixed per request, not proportional to the work.} The catalog sweep
separates the two possibilities, because it varies the work while holding the identity
operation constant. Across a thousandfold increase in catalog size the difference stays
within a narrow band -- 135.8, 124.0, 105.8 and 131.4\,ms for the Fraunhofer ISST
\gls{edc}, and 92.0, 108.8, 72.3 and 36.1\,ms for Factory-X -- while the underlying
latency grows by factors of 1.64 and 1.76. Expressed as a ratio the same cost therefore
falls as the catalog grows, from 1.47 to 1.23 and from 1.16 to 1.03. A verification
performed once per request, independent of how much the request returns, produces exactly
this signature, and its practical consequence is that the relative penalty of running real
identity diminishes as the workload per request increases.

\textbf{The cost is confined to the control plane.} \Cref{fig:rq1-payload} shows the four
data-plane series lying almost on top of one another across five orders of magnitude, with
the largest difference in pull latency being 7.5\,ms on a 100\,MB transfer. Identity is
paid once per transaction and nothing per byte transferred, which is consistent with a
design that establishes trust before the transfer begins and then hands the data plane a
token rather than re-verifying during it~\citep{dcpSpec}.

\textbf{Where the cost lands differs by architecture, and this is the observation that
most constrains an explanation of the connector-to-connector differences.} On both
\gls{edc} connectors the increase concentrates almost entirely in the catalog request,
which rises by factors of 12.2 and 9.1, while the two asynchronous waits change by between
zero and ten per cent. On DSP-Native BaSyx the pattern inverts: the catalog request rises
by 47.9\,ms, but time-to-agreed rises by a factor of 7.0 and time-to-\gls{edr} by 4.0, so
the cost falls inside the asynchronous state machine rather than ahead of it. The
\gls{edc} pattern is what a design that verifies a presentation once, at the first
protected request of an exchange, and then proceeds against an established context would
produce; the BaSyx pattern is what per-message validation inside the protocol handlers
would produce. Both readings are consistent with the respective documented
designs~\citep{dcpSpec, dspProtocolLib}, and the traces of \cref{subsec:tracing} are the
evidence that would confirm or refute them directly. % TODO: needs source -- confirm against the EDC DCP implementation whether verification is per-exchange or per-message.

One measurement resists this account. Negotiation initiation is consistently \emph{faster}
with identity enabled than without it -- 6.2\,ms against 50.1\,ms on the Fraunhofer ISST
\gls{edc}, 7.0\,ms against 45.8\,ms on Factory-X, and 6.8\,ms against 10.4\,ms on BaSyx.
The effect is large, consistent across connectors and outside run-to-run variation, so it
is not noise. It is reported here as observed rather than explained: the identity-disabled
arms substitute a mock identity service for the real one, and a mock that answers
synchronously on the request path could plausibly cost more at that step than a real
implementation that has already established its context, but this study did not measure
the mock's internal behaviour and cannot distinguish that explanation from alternatives.

\textbf{The BaSyx figure is not comparable with the other two.} Its identity-disabled arm
replaces the \gls{edc} consumer with a callback sink and drives the provider with a
different load generator (\cref{subsec:mock-arm}), so the client-side difference of
2070.5\,ms mixes the identity layer with an entirely different consumer stack. The
provider-side figure of 44.7\,ms is the one that may be compared, and it is measured on
the same component by the same agent in both arms. The ratio of 2.90 remains the largest
of the three connectors, but the absolute cost -- 44.7\,ms against 30.0 and 41.3\,ms --
places all three connectors within the same order of magnitude once the comparison is made
on equal terms.

\section{Threats to Validity}\label{sec:results-validity}

% Chapters 2, 3, 4 and 5 forward-reference this section; keep the label.

Every measurement in this chapter has limits, and this section states them: what the
numbers actually measure, and what could have biased the comparison between the two arms
of a connector. How far the results carry beyond this setup, and what may be concluded
from them, are stated in \cref{sec:conclusion-limitations}.

\subsection{What the Numbers Measure}\label{subsec:tv-construct}

\textbf{Polling rounds every phase up.} The harness detects a state change by asking
repeatedly, every 250\,ms, so a phase that ends between two questions is recorded as
ending at the later one. Each phase duration is therefore an overestimate, by up to one
interval.

The size of that error was measured rather than assumed. Repeating the operating point at
a 1000\,ms interval lengthened the reported end-to-end median by 11--15\,\% on the
\gls{edc} connectors -- close to what half an interval per polled phase predicts. At the
250\,ms interval used throughout, the overstatement is a few hundred milliseconds on a
transaction of four to five seconds. It applies equally to every arm, so it inflates the
absolute latencies without biasing any comparison between them.

\textbf{Failed transactions make latency look better than it was.} A transaction still
unfinished after 30\,s is abandoned, and contributes that 30\,s rather than however long
it would really have taken. Percentiles from a run with failures are therefore lower
bounds. The operating-point figures in \cref{sec:rq1} come from runs with no failures at
all; the ladder in \cref{sec:rq2} does contain them, which is why it is read as evidence
of \emph{where} delivery breaks down and not as a latency measurement.

\textbf{Measuring costs the connector something.} About sixty per cent of the requests
each connector answered at the operating point were the harness asking whether a phase had
finished. That load falls on the consumer, not the provider. The provider-side figures in
\cref{tab:x1-provider} are consequently the least disturbed by the act of measuring, which
is a further reason to prefer them when comparing connectors.

\subsection{What Could Have Biased the Comparison}\label{subsec:tv-internal}

\textbf{Some scenarios ran once.} The operating point was repeated five times per arm and
the load ladder three times, but the concurrency ladder and the endurance run once each.
Those two carry no variability estimate, and their figures are single observations.

\textbf{Scenarios always ran in the same order.} Randomising would have averaged out any
carry-over between them, but it would also have let a connector's result depend on which
scenario happened to precede it, which is the larger risk (\cref{subsec:orchestration}).
The age of the running stack is recorded with every run, so a carry-over effect can be
tested for afterwards rather than assumed absent.

\textbf{Three differences between arms were not eliminated.} The DSP-Native BaSyx
identity-disabled arm differs from its partner in the consumer stack and the load driver
as well as in the identity layer, which is why its end-to-end overhead is excluded from
cross-connector comparison and the provider-side figure used instead. The endurance runs
were repeated after a load-generator configuration defect was corrected, so they carry a
different driver fingerprint from the rest. And the instrumentation agent has a cost that
was not measured separately here; because the same agent runs everywhere, it is a constant
of the comparison rather than something that varies by connector.

\textbf{Server errors occurred, and were rare.} They account for 0.18\,\% of requests on
the BaSyx identity-enabled consumer, up to 0.04\,\% on Factory-X, and none at all on the
Fraunhofer ISST \gls{edc} across fifty runs. Almost all occur in the ramped scenarios,
past the point where delivery had already broken down.


\section{Summary}\label{sec:results-summary}

This chapter reports the measurements that answer the five research questions of
\cref{sec:gqm-decomposition}, together with the cross-goal result X1 that follows from
them. At a realistic operating point the two \gls{edc} connectors were indistinguishable
from one another and DSP-Native BaSyx completed the same transaction in roughly three
fifths of their time. Under rising load the ordering changed: Factory-X was the first to
stop converting offered load into completed transactions, the Fraunhofer ISST \gls{edc}
plateaued, and BaSyx continued to deliver at every rung tested. None of the three
approached the capacity of the host, so each ceiling is a property of the connector rather
than of the machine.

Decomposed by phase, between 93 and 97\,\% of every transaction was spent waiting for the
two asynchronous state machines to reach their terminal states, which places the protocol
rather than the identity layer at the baseline. Identity itself cost a fixed amount per
request, confined to the control plane and independent of the work the request performed,
but it landed in a different phase on the \gls{edc} connectors than on BaSyx. That
difference is the observation \cref{sec:answers-rq} carries into the architectural
interpretation, and \cref{sec:conclusion-limitations} states how far these results carry
beyond the setup that produced them.
